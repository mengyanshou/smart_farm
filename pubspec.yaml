name: smart_farm
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mqtt_client: ^8.1.0
  # svg图像
  flutter_svg: ^0.19.1
  # Flutter新开放的共享元素动画
  animations: ^1.0.0+5

  # 屏幕适配
  flutter_screenutil: ^2.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/icon/
